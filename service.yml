apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-deployment
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 80
    name: http
  selector:
    app: nginx-deployment
